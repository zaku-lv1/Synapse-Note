<%- include('partials/header') %>

<div class="auth-container">
    <div class="auth-card">
        <div class="auth-header">
            <h2>初回管理者登録 (デモ)</h2>
            <p>システムの初回管理者を登録します。管理者キーが必要です。</p>
        </div>

        <div class="info-message">
            デモ表示: 実際の使用にはFirebase設定が必要です
        </div>

        <form class="auth-form">
            <div class="form-group">
                <label for="username">ユーザー名</label>
                <input type="text" id="username" name="username" required 
                       placeholder="表示名を入力してください" maxlength="50" value="システム管理者">
            </div>

            <div class="form-group">
                <label for="handle">ハンドル名</label>
                <div class="handle-input">
                    <span class="handle-prefix">@</span>
                    <input type="text" id="handle" name="handle" required 
                           placeholder="ユニークなIDを入力" pattern="[a-zA-Z0-9_]+" 
                           title="英数字とアンダースコアのみ使用可能" maxlength="30" value="admin">
                </div>
                <small class="form-hint">英数字とアンダースコア(_)のみ使用可能</small>
            </div>

            <div class="form-group">
                <label for="password">パスワード</label>
                <input type="password" id="password" name="password" required 
                       placeholder="安全なパスワードを設定してください" minlength="8" value="password123">
                <small class="form-hint">8文字以上で設定してください</small>
            </div>

            <div class="form-group">
                <label for="adminKey">管理者キー</label>
                <input type="password" id="adminKey" name="adminKey" required 
                       placeholder="管理者キーを入力してください" value="admin-setup-key-12345">
                <small class="form-hint">システム管理者から提供された管理者キーを入力</small>
            </div>

            <button type="button" class="auth-submit" onclick="alert('デモ: 実際の環境では管理者登録が実行され、管理者ダッシュボードにリダイレクトされます')">管理者として登録</button>
        </form>

        <div class="auth-links">
            <a href="/">トップページに戻る</a>
        </div>
    </div>
</div>

<style>
.handle-input {
    position: relative;
    display: flex;
    align-items: center;
}

.handle-prefix {
    position: absolute;
    left: 16px;
    color: #666;
    font-weight: 500;
    z-index: 1;
}

.handle-input input {
    padding-left: 32px;
}

.form-hint {
    display: block;
    margin-top: 4px;
    color: #666;
    font-size: 0.85em;
}
</style>

<%- include('partials/footer') %>